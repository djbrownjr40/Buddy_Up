<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path "hiking.jpg" %>);">
  <div class="container">
    <h1>Welcome to buddyアップ!</h1>
    <p>Meet new friends through activities!</p>
    <p>Need a plus 1 for a wedding?</p>
    <p>Or just looking to earn some coin for your spare time?</p>
  </div>
</div>

<div class="container mt-5 gray-background">
  <h2 class="text-center">How buddyup works:</h2>

  <div class="row">
    <div class="col-md-6">
      <div class="search-details px-4 py-4">
        <h3>Find new friends through your favorite hobbies, sports, and games...</h3>
      </div>

      <div class="search-bar">
        <form class="d-flex flex-row px-4" action="<%= activities_path %>" method="GET">
          <input class="form-control mr-sm-2 small-search-input" type="search" placeholder="hiking" aria-label="Search" name="query">
          <button class="btn button-link my-2 my-sm-0" type="submit">go!</button>
        </form>
      </div>
    </div>

    <div class="col-md-6">
      <div class="bg-light sticky-top px-4 py-4">
        <h1 class="text-center">...or add a new activity!</h1>
        <div class="activity-form">
          <%= simple_form_for @activity do |f| %>
            <%= f.input :name, required: true, class: "short-input" %>
            <%= f.input :location, required: true, class: "short-input" %>
            <%= f.input :description, required: true %>
            <%= f.input :hourly_rate, required: true, class: "short-input" %>
            <%= f.input :photo, as: :file %>
            <%= f.button :submit, "post your activity!", class: "btn button-link btn-block d-flex center-align justify-content-center" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
